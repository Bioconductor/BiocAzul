% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importToTerra.R
\name{importToTerra}
\alias{importToTerra}
\title{Import Data from the Human Cell Atlas Data Portal or the AnVIL Data
Repository to a Terra Workspace}
\usage{
importToTerra(api, namespace, name, filters, catalog, format = "terra.pfb")
}
\arguments{
\item{api}{\code{Azul} object representing the connection to the API.}

\item{namespace}{\code{character(1)} AnVIL workspace namespace as returned by,
e.g., \code{AnVILGCP::avworkspace_namespace()}, where the data will be imported.}

\item{name}{\code{character(1)} AnVIL workspace name as returned by, e.g.,
\code{AnVILGCP::avworkspace_name()}, where the data will be imported.}

\item{filters}{\code{list()} A list of filters to apply when preparing the
manifest. The filters should be structured according to the requirements of
the Human Cell Atlas API, and will be converted to JSON format before being
sent in the request to the API. See the details section for more
information on the expected structure of the filters.}

\item{catalog}{\code{character(1)} specifying the catalog to query. Options are
given by \code{listCatalogs(api)}.}

\item{format}{\code{character(1)} The format of the manifest to be prepared.
Currently, only "terra.pfb" is supported, which prepares the manifest in a
format suitable for import into Terra.}
}
\value{
Called for the side effect of importing data into the specified
Terra workspace. If successful, returns a list with the following elements:
\itemize{
\item \code{jobId}: The unique identifier for the import job created in Terra.
\item \code{status}: The final status of the import job, which should be "Done" if
the import was successful.
\item \code{elapsed}: The total time taken for the import job to complete, in
seconds.
}
}
\description{
This function is intended to facilitate the import of data from
the Human Cell Atlas Data Portal or the AnVIL Data Repository. It is
designed to create a manifest for the specified filters and format, and
then import the data into a Terra workspace. The function takes advantage
of the \code{Azul} class to interact with the Human Cell Atlas API and the
\code{AnVIL} package to manage the Terra workspace and import job.
}
\details{
The \code{filters} parameter should be a list that specifies the criteria
for selecting the data to be imported.

Each filter consists of a field name, a relation (relational operator), and
an array of field values. The available relations are "is", "within",
"contains", and "intersects". Multiple filters are combined using "and"
logic. An entity must match all filters to be included in the response. How
multiple field values within a single filter are combined depends on the
relation.

For the "is" relation, multiple values are combined using "or"
logic. For example, \code{list(fileFormat = list(is = c("fastq", "fastq.gz")))}
selects entities where the file format is either "fastq" or "fastq.gz". For
the "within", "intersects", and "contains" relations, the field values must
come in nested pairs specifying upper and lower bounds, and multiple pairs
are combined using "and" logic. For example,
\code{list(donorCount = list(within = matrix(c(1, 5, 5, 10), 2L, 2L, TRUE)))}
selects entities whose donor organism count falls within both ranges, i.e.,
is exactly 5. The accessions field supports filtering for a specific
accession and/or namespace within a project. For example,

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
    accessions = list(
        is = data.frame(namespace = "array_express")
    )
)
}\if{html}{\out{</div>}}

will filter for projects that have an \code{array_express}
accession. Similarly,

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
    accessions = list(
        is = data.frame(accession = "ERP112843")
    )
)
}\if{html}{\out{</div>}}

will filter for projects that have the accession \code{ERP112843} while

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
    accessions = list(
        is = data.frame(
            namespace = "array_express",
            accession = "E-AAAA-000"
        )
    )
)
}\if{html}{\out{</div>}}

will filter for projects that match both values. The \code{organismAge} field is
special in that it contains two property keys: \code{value} and \code{unit}. For
example,

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
    organismAge = list(
        is = data.frame(value = "20", unit = "year")
    )
)
}\if{html}{\out{</div>}}

Both keys are required. \code{list(organismAge = list(is = NA))} selects
entities that have no organism age.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
hca <- Azul(provider = "hca")
catalogs <- listCatalogs(hca)
latest <- head(catalogs, n = 1)
importToTerra(
    hca,
    namespace = "anvil-namespace",
    name = "my-anvil-workspace",
    catalog = latest,
    filters = list(
        projectId = list(is = "74b6d569-3b11-42ef-b6b1-a0454522b4a0")
    )
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=makeFilter]{makeFilter()}}
}
